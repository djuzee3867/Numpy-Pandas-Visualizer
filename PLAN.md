# numpy / pandas visualizer — แผนงาน

เครื่องมือแนว [Python Tutor](https://pythontutor.com/visualize.html) แต่โฟกัสที่ numpy array
และ pandas DataFrame: พิมพ์โค้ดในเว็บ กด Visualize แล้วกดปุ่มไล่ดูทีละสเต็ปว่าข้อมูลเปลี่ยนยังไง

**วิธีรัน:** `python server.py` แล้วเปิด http://127.0.0.1:5000

## หลักการทำงาน

```
[textarea โค้ด] --POST /api/trace--> server exec ด้วย sys.settrace
                                            |
                                     snapshot ทุกบรรทัด
                                            |
     <-- JSON: [step0, step1, step2, ...] ทั้งชุดกลับมาทีเดียว
                                            |
                              JS เก็บไว้ กดปุ่ม Next/Prev ไล่ดู (ไม่ยิง server ซ้ำ)
```

ส่ง trace กลับทั้งชุดครั้งเดียว → server ไม่ต้องเก็บ session (วิธีเดียวกับ Python Tutor)

## โครงไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `server.py` | Flask เสิร์ฟหน้าเว็บ + endpoint `/api/trace` — **จุดที่รัน** |
| `tracer.py` | exec โค้ดผู้ใช้ด้วย `sys.settrace` + timeout + จำกัดจำนวนสเต็ป |
| `snapshot.py` | แปลง **ค่าหนึ่งค่า** (ndarray / DataFrame / Series / scalar) → JSON |
| `heap.py` | แปลง**เฟรมทั้งสาย**เป็น frames + heap ตาม identity — หัวใจของเฟส 3 |
| `explainers/` | ตัวขยายสเต็ปย่อยรายโอเปอเรชัน — `base.py` เครื่องมือกลาง + การ์ด `is_safe`, `__init__.py` ทะเบียน, ที่เหลือโมดูลละ 1 โอเปอเรชัน |
| `static/app.js` | วาด grid, ระบายสีช่องที่เปลี่ยน, ปุ่มเดินสเต็ป |
| `templates/index.html` | ช่องพิมพ์โค้ด + ปุ่ม Visualize + Prev/Next + dropdown ตัวอย่าง |

---

# เฟส 1 — ตัวใช้งานได้จริง (trace ระดับบรรทัด)

- [x] **Step 0 · วางโครงโปรเจกต์** `[S]`
      `requirements.txt`, `server.py` เสิร์ฟหน้าเปล่า, โฟลเดอร์ `static/` `templates/`
      *เช็ค:* `python server.py` → เปิด `127.0.0.1:5000` เห็นหน้า ไม่ error

- [x] **Step 1 · `snapshot.py`** `[M]`
      รับค่า Python → คืน dict ที่ frontend วาดได้ รองรับ ndarray (shape, dtype, ธง view/copy),
      DataFrame, Series, scalar, list, dict, อื่นๆ → `repr()` · ตัดข้อมูลที่ 20×20 พร้อมธง `truncated`
      *เช็ค:* `python tests/smoke.py`
      *ทำก่อนเพราะ:* ทุกชั้นที่เหลือแขวนบนฟอร์แมตนี้ ออกแบบผิดต้องรื้อทั้งกอง

- [x] **Step 2 · `tracer.py`** `[L]` ← ยากสุดของเฟส 1
      exec ใน namespace เปล่า, callback เก็บเลขบรรทัด + locals ทุกบรรทัด, จับ stdout รายสเต็ป,
      จับ exception เป็นสเต็ปสุดท้าย · **การ์ด:** 500 สเต็ป + timeout 5 วิ กัน infinite loop
      *เช็ค:* `python tracer.py examples/demo.py` พิมพ์ JSON ออกมาโดยยังไม่ต้องมีเว็บ

- [x] **Step 3 · `/api/trace`** `[S]`
      POST `{code}` → tracer → JSON, โค้ดพังคืน error ใน payload ไม่ใช่ 500
      *เช็ค:* `curl` ยิงเข้าไป

- [x] **Step 4 · ต่อวงจรให้ครบ (ยังไม่สวย)** `[S]`
      textarea + ปุ่ม Visualize → fetch → **โยน JSON ดิบขึ้นหน้าจอ**
      *เช็ค:* เห็น JSON บนเว็บ = วงจร browser↔server ครบ ที่เหลือคืองานวาดล้วนๆ

> 🔍 **จุดรีวิวที่ 1** — หลัง Step 4  ✅ ถึงแล้ว
>
> ผ่าน: `python tests/smoke.py` (38 เช็ค) · `python tracer.py examples/demo.py` · คลิก Visualize ในเบราว์เซอร์จริง

> **เปลี่ยนทิศตอน Step 5:** UI ทั้งหมดเป็น **ภาษาอังกฤษ** และเลย์เอาต์เลียนแบบ Python Tutor
> คือแยกคอลัมน์ **Frames** (ชื่อตัวแปร) กับ **Objects** (ก้อนข้อมูล) แล้วลากเส้น SVG เชื่อมกัน
> พร้อมลูกศรเขียว *line that just executed* และชมพู *next line to execute* ที่รางซ้ายของโค้ด

- [x] **Step 5 · วาด grid + เลย์เอาต์ Frames / Objects** `[M]`
      JSON → ตาราง, ndarray 1D/2D/3D, DataFrame, Series, list, dict, scalar
      ค่าพื้นฐานวาดในเฟรมเลย ที่เหลือได้กล่องของตัวเองในคอลัมน์ Objects

- [x] **Step 6 · ปุ่มเดินสเต็ป** `[S]`
      `<< First / < Prev / Next > / Last >>` + slider + `Step 3 of 8` + ←/→/Space/Home/End

- [x] **Step 7 · diff ระหว่างสเต็ป** `[M]` ← หัวใจของความเป็น visualizer
      ระบายสีช่องที่ค่าเปลี่ยน (มี flash), ป้าย `new` / `changed`, ป้าย shape เก่า → ใหม่
      ข้ามสโคปไม่เทียบ เพราะตัวแปรคนละชุดจะกลายเป็น new ทั้งกระดาน

> 🔍 **จุดรีวิวที่ 2** — หลัง Step 7  ✅ ผ่านแล้ว

- [x] **Step 8 · renderer ฝั่ง pandas** `[M]`
      หัวคอลัมน์ + แถวบอก dtype + index, `NaN` เป็นตัวเอียงสีจาง ไม่ปนกับ 0

- [x] **Step 9 · เส้นโยง view / copy** `[M]`
      `np.shares_memory` → ป้าย `⚭ shares memory with a` + **เส้นประสี** เชื่อมสองก้อนใน Objects
      แต่ละกลุ่มที่แชร์กันได้สีของตัวเอง

- [x] **Step 10 · panel stdout + error** `[S]`
      `print()` สะสมถึงสเต็ปปัจจุบัน (ตัดจาก `stdout_len`) + แบนเนอร์ error/timeout + ไฮไลต์บรรทัดที่พัง

- [x] **Step 11 · ตัวอย่างสำเร็จรูป + จัดหน้าตา** `[M]`
      dropdown 6 ตัวอย่าง + ธีม 3 สถานะ (ตามระบบ / สว่าง / มืด) จำค่าใน localStorage

> 🔍 **จุดรีวิวที่ 3** — จบเฟส 1  ✅ ถึงแล้ว
>
> ผ่าน: `python tests/smoke.py` (41 เช็ค) · ทดสอบในเบราว์เซอร์จริงครบทั้ง numpy, pandas,
> เคส error, ธีมสว่าง/มืด และเลย์เอาต์ 1 คอลัมน์ / 2 คอลัมน์

---

# เฟส 2 — สเต็ปย่อยรายโอเปอเรชัน

- [x] **Step 12 · โครง explainer framework** `[L]` ← ตัวยากของเฟส 2 อยู่ที่นี่ที่เดียว
      `explainers/` อ่าน AST ของบรรทัดที่ *กำลังจะ* รัน ถ้าตรงรูปแบบที่ลงทะเบียนไว้
      ก็คำนวณขั้นกลางเองแล้วแทรกเป็นสเต็ป `event="explain"` ที่ชี้กลับไปหาสเต็ปแม่ด้วย `parent`
      สเต็ปย่อยไม่มีสถานะของตัวเอง — ยืมภาพ Frames/Objects จากสเต็ปแม่ แล้วโชว์แผงอธิบายทับด้านบน

      **สองกลไกที่ทำให้ทั้งเฟสนี้เป็นไปได้:**
      1. *เคล็ด index array* — เอา `np.arange(size).reshape(shape)` ใส่เข้าโอเปอเรชันเดิม
         แทนข้อมูลจริง แล้วดูว่าเลขไปโผล่ตรงไหน = รู้ว่าช่องไหนถูกเลือก / ย้ายไปอยู่ไหน
         ใช้ได้กับ slicing, fancy indexing, boolean mask, reshape, transpose ด้วยโค้ดชุดเดียว
      2. *การ์ด `is_safe`* — explainer ต้อง eval นิพจน์ของผู้ใช้ซ้ำ จึงอนุญาตเฉพาะนิพจน์ที่
         รันซ้ำแล้วผลเหมือนเดิมและไม่มี side effect (รายชื่อเมธอดปลอดภัยใน `base.py`)
         บรรทัดอย่าง `make().reshape(...)` หรือ `np.random.rand(6)` จะไม่ถูกอธิบายเลย

- [x] **Step 13 · indexing / slicing** `[M]` — ระบายช่องที่ถูกเลือก + บอกว่าผลเป็น view หรือ copy
      ครอบคลุม `a[key]` และ `a[mask] = value` (บอกว่ากำลังจะเขียนทับช่องไหน)
- [x] **Step 14 · broadcasting** `[M]` — เรียง shape ชิดขวา → ยืดทั้งสองฝั่ง → คำนวณ
- [x] **Step 15 · reshape / transpose** `[M]` — โชว์ลำดับ flat ก่อน แล้วโชว์ว่าช่องใหม่มาจากช่องไหน
- [x] **Step 16 · reduce ตาม axis** `[M]` — ทีละกลุ่ม ไฮไลต์ช่องที่ยุบรวมกันเป็นค่านั้น
- [x] **Step 17 · groupby** `[L]` — split → ทีละกลุ่ม → combine
      ถ้าจัดกลุ่มแล้วเลือกคอลัมน์เดียว (`df.groupby("t")["v"]`) จะสร้างคอลัมน์คีย์ขึ้นมาโชว์เอง
      เพราะ SeriesGroupBy ไม่เก็บ DataFrame ต้นทางไว้ ไม่งั้นดูไม่ออกว่าทำไมแถวพวกนี้อยู่กลุ่มเดียวกัน
- [x] **Step 18 · merge / join** `[L]` — ไฮไลต์คอลัมน์คีย์ → แถวไหนหาคู่เจอ/ไม่เจอ → ผลลัพธ์
- [x] **Step 19 · pivot / melt** `[M]` — คอลัมน์ไหนมีบทบาท → wide ↔ long

> 🔍 **จบเฟส 2** — `python tests/smoke.py` ผ่าน 62 เช็ค (เพิ่มหมวด explainer 21 เช็ค)
> ตรวจในเบราว์เซอร์จริงครบทุก explainer

---

# เฟส 3 — รองรับ Python ทั่วไปเต็มรูปแบบ

เดิมภาพถูกออกแบบมาเพื่อ ndarray/DataFrame ซึ่งแชร์กันระดับ *บัฟเฟอร์*
แต่ Python ทั่วไปเป็น *กราฟของ object* — `a = b` ต้องเห็นเป็นสองชื่อชี้กล่องเดียวกัน

- [x] **Step 20 · heap ตาม identity** `[L]` ← ตัวยากที่สุดของทั้งโปรเจกต์
      `heap.py` เก็บ object ทุกตัวไว้ใน heap เดียว อ้างด้วย ref (`{"ref": "o3"}`)
      ส่วนค่าพื้นฐานฝังในเฟรมเลย (`{"inline": ...}`) — สเต็ปหนึ่งจึงเป็น `frames` + `heap`
      แทน `vars` แบบเดิม · จำ object ด้วย `id()` และ **ถือ reference ไว้ตลอด trace**
      เพื่อไม่ให้ id ถูกใช้ซ้ำหลัง gc · เพดานสองชั้น: 50 ช่อง/คอนเทนเนอร์ และ 80 object/heap
      *ผลพลอยได้:* `list` ซ้อน `list` วาดเป็นกล่องซ้อนที่มีลูกศร, linked list และ object ที่ชี้
      กลับมาหาตัวเองก็วาดได้ไม่วนไม่รู้จบ

- [x] **Step 21 · กล่อง instance / class / function** `[M]`
      instance อ่าน `__dict__` หรือ `__slots__` เป็นตารางแอตทริบิวต์,
      class โชว์เมธอดเป็น ref, function โชว์ `ชื่อ(พารามิเตอร์)` แทน `<function at 0x...>`

- [x] **Step 22 · call stack** `[M]`
      เก็บ locals ของ **ทุกเฟรมในสาย** ไม่ใช่แค่เฟรมปัจจุบัน แล้ววาดซ้อนกัน
      เฟรมในสุดขอบเน้นสี — recursion จึงเห็นครบทุกชั้น

- [x] **Step 23 · return value + ของประกอบ** `[S]`
      ดัก event `return` เพิ่มสเต็ปที่มี slot พิเศษ `Return value`
      · ปุ่ม `Reset` · `wrap` · สไลเดอร์ขนาดฟอนต์
      · ตัวอย่างชุด Python 9 อัน (swap, loop, aliasing, grid, dict, bubble sort,
      recursion, class, linked list) แยกกลุ่มจาก numpy/pandas ใน dropdown

- [x] **Step 24 · editor แก้ได้ตลอดเวลา** `[M]`
      เลิกใช้โหมดดู/โหมดแก้แยกกัน (ไม่มีปุ่ม "Edit this code" อีกแล้ว) เหลือ textarea ตัวเดียว
      แล้ววาดแถบไฮไลต์บรรทัดไว้ *ข้างหลัง* ด้วย `#hl-layer` ซึ่งถือข้อความชุดเดียวกัน
      แต่สีโปร่งใส — ตัดบรรทัดเหมือนกันเป๊ะ แถบจึงตรงกับข้อความแม้เปิด wrap
      · เลขบรรทัด + ลูกศรเขียว/ชมพูอยู่ในรางซ้าย เลื่อนตาม textarea
      · พิมพ์แก้เมื่อไหร่ แถบไฮไลต์ล้างทันทีพร้อมข้อความว่าให้กด Visualize ใหม่
      · ย้าย Examples / wrap / ขนาดฟอนต์ มาไว้ในหัวแผง CODE

      > ⚠ textarea กับ `#hl-layer` ต้องใช้ font / size / line-height / padding ชุดเดียวกัน
      > แก้ฝั่งใดฝั่งหนึ่งแล้วต้องแก้อีกฝั่งด้วย ไม่งั้นแถบจะเลื่อนไม่ตรงบรรทัด

- [x] **Step 25 · ปรับขนาดช่องได้ + จัดแถบเดินสเต็ป** `[M]`
      ตัวลาก 2 ตัว: แนวตั้งคั่นคอลัมน์โค้ดกับ heap (ขอบขวาของช่องโค้ด),
      แนวนอนคั่นช่องโค้ดกับ stdout (ขอบล่างของช่องโค้ด = ขอบบนของ stdout)
      เก็บค่าไว้ใน localStorage · ระหว่างลากเรียก `drawWires()` ตลอด เส้นโยงจะได้ไม่หลุด
      · แถบเดินสเต็ปแยกเป็น 2 แถว: สไลเดอร์เต็มความกว้างข้างบน แล้วปุ่มอยู่กึ่งกลางจริง
      ด้วยกริด `1fr auto 1fr` (สถานะทางขวายาวแค่ไหนปุ่มก็ไม่เลื่อน)
      · `stdout` สูงเริ่มต้น 190px และช่องโค้ดมี `min-height` 200px กันโดนบีบจนอ่านไม่ได้

- [x] **Step 26 · `input()` + เก็บรายละเอียดหน้าตา** `[M]`
      **รองรับ `input()` แล้ว** — โค้ดเรียก `input()` แล้วค่าหมด tracer จะหยุดแล้วคืน
      `awaiting_input: {prompt}` frontend โชว์แผงถาม พอผู้ใช้ตอบก็เก็บใส่ list แล้ว
      **รันโค้ดใหม่ทั้งชุด**พร้อมค่าที่สะสมไว้ (วิธีเดียวกับ Python Tutor)
      → server ไม่ต้องเก็บ session เลย และผลลัพธ์คงเดิมทุกครั้งที่รันซ้ำ
      · แก้โค้ดหรือกด Visualize ใหม่ = ล้างคำตอบเก่าทิ้ง เพราะไม่เข้าคู่กับ `input()` ตัวเดิมแล้ว
      · ช่องกรอกอยู่ใน `<form>` เพื่อให้ Enter ส่งได้ตามพฤติกรรมมาตรฐานของเบราว์เซอร์
      · ช่องที่ค่าเปลี่ยนเปลี่ยนเป็น**สีเขียว**ชุดเดียวกับบรรทัดที่เพิ่งรัน (ป้าย `changed` ยังเหลือง)
      · ลูกศรรางซ้ายเหลือช่องเดียว ใช้ `➡` ตำแหน่งเดียวกันทุกบรรทัด แถบไฮไลต์คลุมถึงรางเลขบรรทัด
      · ตัวลากปรับขนาดไม่มีเส้นให้เห็นจนกว่าจะเอาเมาส์ไปชี้
      · เอา "up to the current step" กับป้าย `Ctrl` `Enter` ออก

- [x] **Step 27 · หน้าเริ่มต้นสะอาด + ตัวลากมีขีดจับ** `[S]`
      หน้าแรกไม่มีโค้ดตั้งต้นแล้ว เหลือข้อความจางๆ ชี้ทาง (`# Write Python here…`)
      · ปุ่ม **Reset = กลับไปหน้าเริ่มต้นจริงๆ** — ล้างโค้ด ล้าง trace ล้างคำตอบ input
      และคืนขนาดช่องเป็นค่าเริ่มต้น (ลบทั้ง inline style และค่าใน localStorage)
      · ตัวลากปรับขนาดโชว์เป็น**ขีดสั้นๆ ตรงกลาง** ให้รู้ว่าลากได้ (จับได้ตลอดแนวเหมือนเดิม)
      · ช่องโค้ดว่างแล้วกด Visualize จะไม่ยิง server ให้เสียเที่ยว แต่บอกให้เขียนโค้ดก่อน

- [x] **Step 28 · เก็บงานหน้าตารอบสุดท้าย** `[S]`
      · **ระยะห่างระหว่างแผงเหลือ 12px เท่าขอบนอก** — เดิม `gap: 6px` บวกกับคอลัมน์ตัวลาก 10px
      กลายเป็น 22px ดูโหว่ · แก้เป็น `gap: 0` แล้วให้คอลัมน์ตัวลาก (12px) เป็นช่องว่างเสียเอง
      · **ธีมเหลือสองสถานะ สลับคลิกเดียว** — เดิมเป็นวงจร 3 สถานะ (ตามระบบ → สว่าง → มืด)
      ทำให้บางทีต้องกดสองสามทีกว่าภาพจะเปลี่ยน ตอนนี้อ่านธีมของระบบเป็นค่าตั้งต้น
      แล้วคลิกสลับสว่าง/มืดตรงๆ ไอคอนบอกสถานะปัจจุบัน (☀ / ☾)
      · เอาโลโก้สี่เหลี่ยมหน้าชื่อออก เหลือแต่ชื่อแอป

- [x] **Step 29 · วาง object เป็นขั้นบันไดแบบ Python Tutor** `[S]`
      เดิมกล่องทุกใบเรียงชิดซ้ายคอลัมน์เดียว พื้นที่ด้านขวาว่างเปล่าและลูกศรสั้นจนอ่านยาก
      · วางกล่องลูกให้เริ่มถัดจาก **ขอบขวาจริงของกล่องแม่** (วัดหลังกล่องเข้า DOM แล้ว)
      ไม่ใช่เยื้องทีละระยะตายตัว — เยื้องตายตัว 60px ทำให้ลูกยังซ้อนใต้แม่อยู่
      (กล่องกว้าง ~170px) เส้นเลยต้องดิ่งผ่านตัวกล่องเองก่อนวกเข้าเป้า
      พอวางให้พ้นขอบขวา เส้นกลายเป็นเส้นโค้งสั้นๆ ลงขวาในที่ว่าง ไม่พาดทับกล่องไหนเลย
      · linked list กลายเป็นบันได 3 ชั้นตามความยาวของโซ่
      · **ลำดับการวาง = เดิน DFS จากตัวแปรในเฟรม** เจอ object ไหนก็วางต่อท้ายทันที ลูกได้ชั้นถัดไป
      ลูกของกล่องหนึ่งจึงเรียงตรงกับลำดับช่องของมัน **เส้นไม่ไขว้กัน**
      (ลองเรียงตามชั้นล้วนๆ ก่อนแล้ว — กล่องที่มีตัวแปรชี้ตรงจะถูกดึงขึ้นมาก่อนพี่น้อง เส้นพันกันหมด)

- [x] **Step 30 · เส้นโยงออกทางที่ถูก** `[S]`
      จุดที่อยู่ *ในกล่อง* (ช่องของ list, แถวของ instance) ต้องให้เส้น**ออกทางล่าง**
      ไม่ใช่ทางขวา ไม่งั้นเส้นตัดผ่านขอบกล่องตัวเองทุกครั้ง ส่วนจุดในเฟรมยังออกทางขวาเหมือนเดิม
      · ความโค้งคิดจากทั้งระยะแนวนอนและแนวตั้ง เส้นที่ตกลงไกลๆ จะโค้งกว้างพอไม่ทับกล่องอื่น
      · ปลายทางเข้าที่ขอบซ้ายระดับหัวกล่องเสมอ
      · **เส้นวาดทับกล่อง** (`z-index` สูงกว่า) ไม่ใช่ลอดใต้ ไม่งั้นเส้นที่พาดผ่านกล่องอื่น
      จะขาดหายเป็นช่วงๆ จนดูไม่ออกว่าลากไปไหน — `pointer-events: none` จึงยังคลิกของข้างล่างได้

- [x] **กันเซิร์ฟเวอร์ค้าง** `[S]` — สับ `sys.stdin` เป็นค่าว่างระหว่าง exec
      ไม่งั้นโค้ดที่เรียก `input()` จะทำให้ server รอคีย์บอร์ดที่ไม่มีวันมา

> 🔍 **จบเฟส 3** — `python tests/smoke.py` ผ่าน 81 เช็ค
> ตรวจในเบราว์เซอร์: aliasing, nested list, dict, class + instance, linked list,
> call stack ของ recursion และของเดิมฝั่ง numpy/pandas ยังครบ

---

- [x] **Step 31 · คอมเมนต์ในโค้ดเป็นภาษาอังกฤษทั้งหมด** `[S]`
      แปลคอมเมนต์ docstring และ label ของเทสต์ทุกไฟล์เป็นอังกฤษ (394 บรรทัด)
      พร้อมตัดคอมเมนต์ที่พูดซ้ำกับโค้ดทิ้ง เหลือเฉพาะที่อธิบาย *ทำไม* ไม่ใช่ *ทำอะไร*
      · ไฟล์นี้ (PLAN.md) ยังเป็นภาษาไทย เพราะเป็นเอกสารของโปรเจกต์ ไม่ใช่คอมเมนต์ในโค้ด

---

## ข้อจำกัดที่รู้ตัวอยู่แล้ว

1. **`settrace` เห็นแค่ "ระหว่างบรรทัด"** — `df.groupby('k').sum()` = 1 บรรทัด = กระโดดทีเดียวจบ
   เฟส 2 แก้ด้วย explainer รายตัว แต่ **ต้องเขียนเพิ่มทีละโอเปอเรชัน** ไม่มีทางลัดอัตโนมัติ
   โอเปอเรชันที่ยังไม่มี explainer จะเห็นแค่ก่อน/หลัง เหมือนเฟส 1
7. **explainer อธิบายได้เฉพาะบรรทัดที่ประเมินซ้ำได้ปลอดภัย** — ถ้าบรรทัดนั้นเรียกฟังก์ชันของผู้ใช้เอง
   หรือมีค่าไม่คงที่ (`np.random`) จะข้ามการอธิบายไปเงียบๆ เพื่อไม่ให้ side effect เกิดสองครั้ง
   และไม่ให้ภาพที่อธิบายต่างจากที่รันจริง
8. **บรรทัดแก้ค่าในที่ (`b += 10`) ไม่มี explainer** — รันซ้ำไม่ได้ตามนิยาม
2. **ไม่มี sandbox** — `exec` โค้ดผู้ใช้ตรงๆ ใช้ในเครื่องตัวเอง (bind `127.0.0.1`) เท่านั้น
   **ห้ามเอาขึ้นเซิร์ฟเวอร์สาธารณะ**
3. **timeout เช็คได้เฉพาะระหว่างบรรทัด** — ลูปหนักๆ ในชั้น C ของ numpy จะไม่โดนตัด
4. **NA ทุกชนิดถูกยุบเป็น `null`** — `None` กับ `NaN` จะดูเหมือนกันในตาราง
5. **array เกิน 2 มิติ** — 3D วาดเป็นชุด slice 2D, 4D+ แสดงแค่ไดอะแกรม shape
6. **object ถูกถือ reference ไว้ตลอด trace** (เพื่อไม่ให้ `id()` ถูกใช้ซ้ำ)
   ผลข้างเคียงคือ `__del__` จะไม่ถูกเรียกระหว่างรัน และ refcount ที่เห็นจะสูงกว่าปกติ
   แลกมากับการที่ ref ของแต่ละ object เชื่อถือได้ตลอดทั้ง trace
9. **เพดานภาพ** — 50 ช่องต่อคอนเทนเนอร์ และ 80 object ต่อ heap
   เกินกว่านั้นจะขึ้นหมายเหตุว่ามีของที่ไม่ได้วาด
10. **เปิด wrap แล้วเลขบรรทัดจะถูกซ่อน** — บรรทัดจริงกับบรรทัดที่เห็นไม่ตรงกัน
    (แถบไฮไลต์ยังตรงอยู่ เพราะ `#hl-layer` ตัดบรรทัดตามข้อความจริง)
11. **`input()` ใช้ได้แล้ว** (Step 26) แต่ทุกครั้งที่ตอบ โปรแกรมจะถูก **รันใหม่ทั้งหมด**
    โค้ดที่มี side effect นอกตัวเอง (เช่นเขียนไฟล์) จึงทำงานซ้ำหลายรอบ
    ส่วน `sys.stdin` ตัวจริงยังถูกสับเป็นค่าว่างอยู่ — อ่าน stdin ตรงๆ ยังได้ `EOFError`
